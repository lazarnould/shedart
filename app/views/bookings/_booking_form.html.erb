<%= simple_form_for [@booking] do |f| %>
  <%= f.error_notification %>

  <%= f.input :checkin %>
  <%= f.input :checkout %>
  <%= f.input :application, as: :text %>

  <%= f.button :submit %>
<% end %>
